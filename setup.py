from distutils.core import setup

setup(name='batchsubmit',
      version='0.1',
      py_modules= ['batchsubmit'],
      # data_files = ['share/with-env'],
      # scripts = ['bin/prototool']
      )
